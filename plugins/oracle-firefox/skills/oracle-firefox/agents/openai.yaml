interface:
  display_name: "Oracle Firefox"
  short_description: "Durable ChatGPT Pro consultation via Firefox"
  default_prompt: "Use $oracle-firefox to run a durable Pro review, retry one confirmed response failure, hand completion back automatically, and verify the result."
dependencies:
  tools:
    - type: "mcp"
      value: "oracle-firefox"
      description: "Durable Oracle Firefox broker client"
policy:
  allow_implicit_invocation: true
